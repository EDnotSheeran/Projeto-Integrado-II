{
  "name": "projeto-integrado-ii",
  "version": "1.0.0",
  "description": "projeto integrado ii",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "docker-compose up -d --build app",
    "build": "docker-compose up -d --build app",
    "stop": "docker-compose down",
    "clean": "docker system prune -af",
    "clean-volumes": "docker system prune -f",
    "scan": "docker scan",
    "create": "docker-compose run --rm composer create-project laravel/laravel .",
    "artisan": "docker-compose run --rm artisan",
    "composer": "docker-compose run --rm composer",
    "npm": "docker-compose run --rm npm",
    "controller": "docker-compose run --rm artisan make:controller",
    "model": "docker-compose run --rm artisan make:model",
    "migration": "docker-compose run --rm artisan make:migration",
    "request": "docker-compose run --rm artisan make:request",
    "migrate": "docker-compose run --rm artisan migrate",
    "refresh": "docker-compose run --rm artisan migrate:refresh",
    "watch": "docker-compose run --rm --service-ports npm run watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/EDnotSheeran/Boilerplate-Laravel.git"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/EDnotSheeran/Boilerplate-Laravel/issues"
  },
  "homepage": "https://github.com/EDnotSheeran/Boilerplate-Laravel#readme"
}
